
<%= form_for @song do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :year_of_release %>
  <%= f.number_field :year_of_release %>

  <%= f.submit (@song.new_record? ? "Create" : "Update") %>
<% end %>
